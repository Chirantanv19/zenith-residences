import ContactForm from "@/components/sections/ContactForm";
// Ensure no unused 'styles' variables are hanging around here
export default function ContactPage() {
  return (
    <main style={{ background: '#0a0a0a' }}>
      <ContactForm />
    </main>
  );
}